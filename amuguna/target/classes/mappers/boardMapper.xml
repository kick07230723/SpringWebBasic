<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
  
  <mapper namespace="org.zerock.persistance.BoardDAO">
  <insert id="create" parameterType="org.zerock.domain.BoardVO">
  insert into tbl_board (title, content)values(#{title},#{content})
  </insert> <!-- insert에서 중요한것은 파라미터 타입(누가 담고있는가)-> DAO -->
  </mapper>